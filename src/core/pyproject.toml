[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "core"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "pandas>=2.3.3",
    "numpy",
]

[tool.setuptools]
package-dir = { core = "." }
packages = [
    "core",
]
